# Task 04: Dynamic Halftone Generator & Block Editor Controls

## Scope & Checklists

- [x] Create `assets/js/halftone-generator.js` with dynamic circular concentric and rectangular concentric templates.
- [x] Implement consistent, uniform 20px dot spacing across rings.
- [x] Provide configurable controls for central dot max size, pattern spread (default 60px), and shrink factor (default 0.88).
- [x] Create `assets/js/halftone-editor.js` registering a Gutenberg Block Inspector panel for `core/image`.
- [x] Include controls for template selection, spread, max dot size, shrink factor, and dot color picker.
- [x] Hook `enqueue_block_editor_assets` and `render_block_core/image` in `functions.php`.
- [x] Connect dynamic halftone generation with mouse repel physics and `ResizeObserver`.
- [x] Run verification tests for Phase 04 and create an atomic Git commit.
